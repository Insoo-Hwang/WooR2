<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="ButtonShadow">
        <item name="android:background">@drawable/shadow</item>
        <item name="android:layout_marginBottom">20dp</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginLeft">20dp</item>
        <item name="android:layout_marginRight">20dp</item>
    </style>
    <style name="omu">
        <item name="android:fontFamily">@font/omu</item>
    </style>
    --- themes.xml
    <!-- Base application theme. -->
    <style name="Widget.MaterialComponents.TextView." parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="android:textViewStyle">@style/omu</item> <!-- android:textViewStyle을 새로만든 MyTextViewStyle로 덮어씌움 -->
    </style>
</resources>